/**
 * Created by andreashailu on 12/28/16.
 */
/*
class Car {
  // Field - variables
  engine: string;
  private _engine: string; // Class values are public by default. '_' prefix is a js convention
  ...
  // Constructor
  constructor(engine: string) {
    this.engine = engine;
  }
  constructor(public engine: string) { }
  ^^ This creates a field of type string called 'engine' for you automatically and sets it to the value passed in
  ...
  //  Properties - get/set fields
  ...
  // Functions
  ...
}

class Engine {
  constructor Engine(public horsePower: number, public engineType: string) {}
}

class Car {
  private _engine: Engine;

  constructor(engine: Engine) {
    this._engine = engine;
  }
  ...
}

var engine: Engine = new Engine(300, 'V8');
var car: Car = new Car(engine);
*/
//# sourceMappingURL=Classes.js.map